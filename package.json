{
  "name": "electronic-betting-system",
  "version": "1.0.0",
  "description": "Electronic Betting System - Full Stack App",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"cyan,yellow\" --prefix \"[{name}]\" --names \"BACKEND,FRONTEND\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
